# Weekly Report

## <%= last_tuesday %> ~ <%= today %> ・ Development summary

### Completed Tasks

<% repos.each do |repo| %>
  <% next if repo[:changes].empty? %>
#### <%= repo[:name] %>
  <% repo[:changes].each do |pr| %>
- <%= pr %>
  <% end%>
<% end %>
